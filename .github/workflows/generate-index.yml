name: Generate Prompt Index

on:
  push:
    branches: [ main ]
    paths:
      - 'prompts/**/*.md'
      - '.github/prompts/**/*.md'
  pull_request:
    paths:
      - 'prompts/**/*.md'
      - '.github/prompts/**/*.md'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-index:
    name: Generate Prompt Index
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate index
        run: |
          bash scripts/generate-index.sh

      - name: Generate APM primitives
        run: |
          bash scripts/generate-apm-primitives.sh

      - name: Check for changes
        id: git-check
        run: |
          git diff --exit-code INDEX.md README.md .apm/prompts || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push if changed
        if: steps.git-check.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add INDEX.md README.md .apm/prompts
          git commit -m "docs: Update prompt index [skip ci]"
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            branch="${{ github.head_ref }}"
          else
            branch="${GITHUB_REF_NAME:-main}"
          fi
          git pull --rebase origin "$branch"
          git push origin HEAD:"$branch"
